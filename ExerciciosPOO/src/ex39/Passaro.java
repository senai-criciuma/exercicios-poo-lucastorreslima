package ex39;
// Crie uma interface Voador com o método: voar().
// Crie classes Passaro e Aviao que implementem essa interface(seus métodos).
public class Passaro implements Voador{
    @Override
    public void voar() {

    }
}
